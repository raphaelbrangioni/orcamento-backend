apiVersion: v1
kind: Service
metadata:
  name: {{ include "orcamento-backend.fullname" . }}
  labels:
    {{- include "orcamento-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orcamento-backend.selectorLabels" . | nindent 4 }}
